config.json 默认配置文件

start.sh 是启动文件，可以根据实际情况设置相关的参数

kill.sh  是关闭程序的脚本，是根据程序名来寻找进程并kill的，命名程序名的时候要慎重

打包完成之后寻找一个最大的目录，推荐的部署结构如下



项目名称
      ---------cfg  存放配置文件
      ---------log  存放日志
      ---------start.sh 启动文件
      ---------kill.sh 关闭文件

项目启动时候，会有一个重定向文件保存日志（注意定期清理和转存该文件）